const encryptionController = (() => {

  const encrypt = () => {
    // Just use a static phrase and key for now.
  };

  const decrypt = () => {
    // Just use a static phrase and key for now.
  };

  return {

  };
})();